global:
  max:
    ue: 1024

db_uri: {{DB_URI}}
logger:
  file: 
    path: /open5gs/logs/pcrf.log
  level: {{LOG_LEVEL}}

pcrf:
  freeDiameter: /tmp/config/pcrf.conf
  metrics:
    server:
      - address: {{MY_IP}}
        port: 9090